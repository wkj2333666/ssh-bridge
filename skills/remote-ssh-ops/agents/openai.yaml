interface:
  display_name: "Remote SSH Ops"
  short_description: "Operate remote hosts without remote Codex login"
  default_prompt: "Use $remote-ssh-ops to inspect and work on my configured SSH host."
dependencies:
  tools:
    - type: "mcp"
      value: "ssh-bridge"
      description: "Local native bridge to allowlisted OpenSSH hosts"
      transport: "stdio"
